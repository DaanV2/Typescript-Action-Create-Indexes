name: 'Typescript action: create includes'
description: 'Creates includes.ts for a typescript project'
branding:
  icon: activity
  color: blue
inputs:
  folder:
    description: The folder path to start at
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
